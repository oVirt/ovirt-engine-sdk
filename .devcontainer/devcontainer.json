{
    "name": "oVirt-engine-sdk",
    "mounts": [
        "source=${localEnv:HOME}/.m2,target=/home/build/.m2,type=bind,consistency=cached"
    ],
    "build": {
        "dockerfile": "../Dockerfile"
    },
    "remoteUser": "build",
    "updateRemoteUserUID": true
}