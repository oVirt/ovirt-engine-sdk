    def update(self$combined_method_params$):
$docs$
        url = '/$url$'
        url = UrlHelper.replace(
            url,
            $url_identifiers_replacments$
        )

        result = self.__getProxy().update(
            url=UrlHelper.appendParameters(url$url_params_separator$),
            body=ParseHelper.toXml(self.superclass),
            headers=$headers_map_params_str$
        )

        return $entity_broker_type$(
            self.parentclass,
            result,
            self.context
        )

