    def add(self, $resource_to_add_lc$$headers_method_params_str$):
$docs$
        url = '/$url$'

        result = self.__getProxy().add(
           url=url,
           body=ParseHelper.toXml($resource_to_add_lc$),
           headers=$headers_map_params_str$
        )

        return $resource_type$(result, self.context)

