    def $action_name$(self, action=params.Action()$headers_method_params_str$):
$docs$
        url = '/$url$'

        result = self.__getProxy().request(
            method='$method$',
            url=UrlHelper.replace(
                url,
                $url_params$
            ),
            body=ParseHelper.toXml(action),
            headers=$headers_map_params_str$
        )

        return result

