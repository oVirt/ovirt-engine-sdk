    def list(self, $combined_method_params$, **kwargs):
$docs$
        url='/$url$'

        result = self.__getProxy().get(
            url=UrlHelper.appendParameters(url, $url_params$),
            headers=$headers_map_params_str$
        ).get_$getter_name$()

        return ParseHelper.toCollection(
            $entity_broker_type$,
            FilterHelper.filter(result, kwargs),
            context=self.context
        )

