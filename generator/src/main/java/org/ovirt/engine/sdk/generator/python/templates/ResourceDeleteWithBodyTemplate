    def delete(self, $body_type_lc$$body_instance_str$, $combined_method_params$):
$docs$
        url = UrlHelper.replace(
            '/$url$',
            {
                '{$resource_name_lc$:id}': self.get_id(),
            }
        )

        return self.__getProxy().delete(
            url=SearchHelper.appendQuery(
                url,
                $url_params$
            ),
            body=ParseHelper.toXml($body_type_lc$),
            headers=$headers_map_params_str$
        )

